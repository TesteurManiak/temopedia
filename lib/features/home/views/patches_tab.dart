import 'package:flutter/material.dart';

class PatchesTab extends StatelessWidget {
  const PatchesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
